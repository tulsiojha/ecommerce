/** @format */

import { Link } from "react-router-dom";


export default function Nav(params) {
  const { navConfig } = params;
  return (
    <div class="flex flex-row items-center justify-between mb-2 shadow-lg bg-gray-800 fixed z-20 h-12 w-full px-2">
     
    </div>
  );
}
